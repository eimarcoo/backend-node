<%- include("includes/header"); %>

<script>

    function confirmar(mensagem) {
        console.log(event)
        const result = confirm(mensagem);

        if (!result) {
            return false;
        }
    }

</script>

<div class="container">
    <h3>Lista de Contatos</h3>

    <div>
        <a type="button" class="btn btn-primary" href="/agendas/create/">Adicionar Tarefa</a>
    </div>
    <table class="table">
        <thead>
            <tr>
                <th scope="col" style="min-width: 200px">Título</th>
                <th scope="col" style="min-width: 300px">Descrição</th>
                <th scope="col">Concluído</th>
                <th scope="col">Opções</th>
            </tr>
        </thead>
        <tbody>
            <% for(const agenda of agendas) { %>
            <tr class="<%= agendas.done == 1 ? 'concluido' : '' %>">
                <td><%= agenda.title %></td>
                <td><%= agenda.description %></td>
                <td><%= agenda.done == 1 ? "Sim" : "Não" %></td>
                <td>
                    <form action="/agendas/update-status/<%= agenda.id %>" method="POST" style="display: inline-block" onsubmit="return confirmar('Mudar status do contato?')">
                        <input type="hidden" name="done" value="<%= agenda.done %>">
                        <button type="submit" class="btn btn-warning">Status</button>
                    </form>
                    <a type="button" class="btn btn-success" href="/agendas/edit/<%= agenda.id %>">Editar</a>
                    <form action="/agendas/remove/<%= agenda.id %>" method="POST" style="display: inline-block" onsubmit="return confirmar('Tem certeza que deseja excluir esse contato?')">
                        <button type="submit" class="btn btn-danger">Excluir</button>
                    </form>
                </td>
            </tr>
            <% } %>
        </tbody>
    </table>

</div>

<%- include("includes/footer"); %>
